name: Frontend - Build and Deploy

on:
  push:
    branches: [main]
    tags: ['v*']
    paths:
      - 'frontend/**'
      - '.github/workflows/frontend.yml'
  pull_request:
    branches: [main]
    paths:
      - 'frontend/**'
      - '.github/workflows/frontend.yml'

env:
  NODE_VERSION: '20'
  REGISTRY: registry.cn-hangzhou.aliyuncs.com
  NAMESPACE: linj0866
  IMAGE_NAME: arxiv-daily-frontend

jobs:
  # Job 1: 构建前端
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: npm
          cache-dependency-path: frontend/package-lock.json

      - name: Install dependencies
        working-directory: ./frontend
        run: npm ci

      - name: Build frontend
        working-directory: ./frontend
        run: npm run build
        env:
          NODE_ENV: production
          VITE_API_BASE_URL: ${{ secrets.API_BASE_URL }}
          VITE_BASE_PATH: /${{ github.event.repository.name }}/

      - name: Upload build artifacts
        uses: actions/upload-artifact@v4
        with:
          name: frontend-dist
          path: frontend/dist
          retention-days: 7

  # Job 2a: 部署到 GitHub Pages（默认）
  deploy-github-pages:
    needs: build
    runs-on: ubuntu-latest
    if: github.event_name != 'pull_request'
    permissions:
      contents: read
      pages: write
      id-token: write

    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    steps:
      - name: Download build artifacts
        uses: actions/download-artifact@v4
        with:
          name: frontend-dist
          path: dist

      - name: Setup Pages
        uses: actions/configure-pages@v4

      - name: Upload to Pages
        uses: actions/upload-pages-artifact@v3
        with:
          path: dist

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4

  # # Job 2b: 七牛云部署（可选）
  # deploy-qiniu:
  #   needs: build
  #   runs-on: ubuntu-latest
  #   env:
  #     QINIU_ENABLED: ${{ secrets.QINIU_ENABLED }}

  #   permissions:
  #     contents: read

  #   steps:
  #     - name: Download build artifacts
  #       uses: actions/download-artifact@v4
  #       with:
  #         name: frontend-dist
  #         path: dist

  #     - name: Install qshell
  #       run: |
  #         wget -O qshell.tar.gz https://devtools.qiniu.com/qshell-linux-amd64-v2.14.0.tar.gz
  #         tar -xzf qshell.tar.gz
  #         sudo mv qshell-linux-amd64-v2.14.0 /usr/local/bin/qshell
  #         qshell version

  #     - name: Configure qshell
  #       run: |
  #         qshell account \
  #           ${{ secrets.QINIU_ACCESS_KEY }} \
  #           ${{ secrets.QINIU_SECRET_KEY }} \
  #           qiniu-deploy

  #     - name: Upload to Qiniu
  #       run: |
  #         # 上传静态资源
  #         qshell qupload2 \
  #           --src-dir=./dist \
  #           --bucket=${{ secrets.QINIU_BUCKET }} \
  #           --key-prefix=/ \
  #           --overwrite=true \
  #           --check-hash=true \
  #           --thread-count=4 \
  #           --skip-path-prefixes="index.html" \
  #           --include-file-extensions="js,css,png,jpg,jpeg,gif,ico,svg,woff,woff2,ttf,eot"



  #         # index.html 不缓存
  #         qshell rput \
  #           ${{ secrets.QINIU_BUCKET }} \
  #           index.html \
  #           ./dist/index.html \
  #           --overwrite

  #     - name: Refresh CDN cache
  #       run: |
  #         qshell cdnrefresh \
  #           --dirs=${{ secrets.QINIU_DOMAIN }}/

  #     - name: Deploy summary
  #       run: |
  #         echo "## 🚀 Qiniu Deployment" >> $GITHUB_STEP_SUMMARY
  #         echo "" >> $GITHUB_STEP_SUMMARY
  #         echo "- **Bucket**: ${{ secrets.QINIU_BUCKET }}" >> $GITHUB_STEP_SUMMARY
  #         echo "- **Domain**: ${{ secrets.QINIU_DOMAIN }}" >> $GITHUB_STEP_SUMMARY

    # Job 3: 构建并推送 Docker 镜像到 ACR
  build-and-push-image:
    needs: build
    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to Alibaba Cloud ACR
        if: github.event_name != 'pull_request'
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ secrets.ACR_USERNAME }}
          password: ${{ secrets.ACR_PASSWORD }}

      - name: Extract metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: |
            ${{ env.REGISTRY }}/${{ env.NAMESPACE }}/${{ env.IMAGE_NAME }}

          tags: |
            type=raw,value=latest
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}
            type=sha,prefix=

      - name: Build and push image
        uses: docker/build-push-action@v5
        with:
          context: ./frontend

          file: ./frontend/Dockerfile
          platforms: |
            linux/amd64
            linux/arm64

          push: ${{ github.event_name != 'pull_request' }}
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Image digest
        if: github.event_name != 'pull_request'
        run: |
          echo "Frontend image pushed"
          echo "${{ steps.meta.outputs.digest }}"